/**
 * Configuration file for API endpoints
 * 
 * IMPORTANT: This file contains the base URL for API calls.
 * When the frontend is served separately from the backend,
 * all API requests must include the full backend URL.
 * 
 * Development setup:
 * - Backend (Flask API): http://localhost:5000
 * - Frontend (Static files): http://localhost:8000
 * 
 * The API_BASE_URL should point to the backend server.
 */

// Base URL for all API requests
// Change this when deploying to production
const API_BASE_URL = 'http://localhost:5000';

// Export for use in other scripts
// (If using modules, otherwise this is a global variable)
